function goToDiv(x) {
    // var el = document.querySelector(x);
    // var rect = el.getBoundingClientRect(),
    // scrollLeft = window.pageXOffset || document.documentElement.scrollLeft,
    // scrollTop = window.pageYOffset || document.documentElement.scrollTop;

    // console.log('top: ' + rect.top + scrollTop);


    // //console.log(document.querySelector(x).getBoundingClientRect().top);
    // var topOfElement = document.querySelector(x).offsetTop - document.querySelector('header').offsetHeight;
    // window.scrollTo(0,0);
    // window.scroll({ top: topOfElement, behavior: "smooth" });
    // console.log('1');

    //var el = document.querySelector(x);
    //el.scrollIntoView(true);
}